# PAC-MAN Clone — SPEC.md

> Phase 0 산출물. 이 문서는 "무엇을 만들 것인가"를 정의한다. "어떻게 만들 것인가"(game loop 구조, state machine 구현, 자료구조, 파일 분할)는 Phase 1의 PLAN.md에서 다룬다.

## 1. 프로젝트 개요

브라우저에서 바로 플레이 가능한 클래식 PAC-MAN 클론. 개인/학습 목적의 팬 프로젝트이며 상업적 배포는 하지 않는다. Vanilla JS + HTML5 Canvas만 사용하는 완전 정적 사이트로 제작해 정적 호스팅에 배포한다.

## 2. Hard 제약 (위반 불가)

- 외부 라이브러리·프레임워크·CDN 금지. Vanilla JS(ES modules) + Canvas 2D API만 사용.
- 이미지/오디오 등 외부 에셋 파일 없이 전부 코드(도형 드로잉, 필요 시 Web Audio 합성)로 생성. 빌드 스텝 없이도 정적 배포 가능해야 한다.
- 서버 런타임·DB 없음. 순수 정적 사이트.
- 각 Phase 게이트에서 사용자 승인 없이 다음 Phase로 진행하지 않는다.
- 배포·git push·rm·기존 파일 덮어쓰기 등 비가역/외부 영향 작업은 실행 전 반드시 확인받는다.
- 아케이드 오리지널의 정확한 수치(타이밍, 속도 %, 점수표 등)를 단정하지 않는다 — 불확실한 값은 [확인필요]로 표기하고 튜닝 지점을 코드에 남긴다.

## 3. Phase 0 인터뷰 결정 사항

| 항목 | 결정 |
|---|---|
| 미로 레이아웃 | **자체 디자인 미로.** 오리지널의 구조 원칙(좌우 대칭, 좌우 터널 2개, 중앙 고스트 하우스, 4방향 교차로가 있는 그리드)은 따르되 타일 데이터는 새로 설계. IP 유사도·기억 오류 리스크 없이 처음부터 검증 가능한 데이터로 제작. |
| Ghost AI 충실도 | **정통 4종 알고리즘.** Blinky(직접 추적)/Pinky(4칸 앞 매복, 오버플로우 버그 포함)/Inky(Blinky 기준 벡터 반사)/Clyde(거리 기반 추적↔도주 전환)를 각각 다른 targeting 로직으로 구현. Scatter↔Chase 주기 교대 + Power Pellet 시 Frightened 상태까지 재현. 정확한 타이밍/속도 수치는 [확인필요]. |
| 사운드 | **끔(기본값).** 외부 음원 파일 없이도 자산 이슈가 전혀 없는 구성. Web Audio 합성음은 이번 버전 범위 밖(추후 확장 포인트로만 남김). |
| 배포 호스트 | **Vercel.** `vercel` CLI로 정적 배포(`vercel --prod`). |

## 4. 자체 판단 기본값 (Soft — "뻔한" 항목이라 인터뷰 생략, 이견 있으면 언제든 조정)

| 항목 | 기본값 | 비고 |
|---|---|---|
| 조작키 | 방향키(↑↓←→) + WASD 동시 지원 | 흔한 관례, 저리스크 |
| 목숨 | 3 | 아케이드 표준 |
| 점수 저장 | in-memory(현재 세션 점수 + 세션 내 high-score). 새로고침 시 초기화 | 사용자가 지정한 Soft 기본값 그대로. localStorage로 영구 저장하는 건 트리비얼한 후속 확장으로 남김 |
| 승리 조건 | 미로의 모든 dot + power pellet 섭취 시 해당 레벨 클리어 → 다음 레벨로 진행(미로 리셋, 난이도 소폭 상승) | 아케이드는 사실상 무한 레벨 진행. "최종 승리 화면"은 두지 않고 레벨을 계속 이어감 |
| 아트 스타일 | 레트로 픽셀풍, **Canvas 도형(arc/fillRect/path)으로 프로시저럴 드로잉.** 이미지 스프라이트 파일 없음 | 이미지 에셋을 생성할 방법이 없으므로 Hard 제약(외부 에셋 금지)상 사실상 유일한 선택지 |
| 파일 구조 | `index.html` + `src/*.js` ES modules 분리 | 구체적 모듈 분할은 PLAN.md에서 설계 |
| 터치/모바일 컨트롤 | **Out of scope.** 데스크톱 키보드 우선 | 필요 시 후속 작업으로 추가 가능하도록 입력 처리는 분리해서 설계 |

## 5. 게임플레이 요구사항

### 5.1 미로
- 그리드 기반 타일맵(벽/통로/dot/power pellet/고스트 하우스 게이트 구분).
- 좌우 끝을 잇는 터널(warp) 최소 1쌍.
- 중앙에 고스트 하우스(고스트 초기 대기 공간 + 출입 게이트).
- 좌우 대칭 구조로 시각적 클래식함 유지.
- 정확한 치수는 PLAN.md에서 확정(원안 28x31과 동일할 필요 없음 — 자체 디자인이므로 구현·플레이 검증이 쉬운 크기로 설계).

### 5.2 Pac-Man
- 그리드 기반 이동이되 부드러운 픽셀 보간(연속적인 애니메이션)으로 표시.
- 입력은 다음 교차로에서 적용되는 "방향 버퍼링"을 지원(막다른 길에서 미리 다음 방향키를 눌러두면 교차로 도달 시 자동 전환) — 클래식 팩맨 조작감의 핵심.
- 입에 여닫는 애니메이션(도형 각도 변화로 표현).
- dot/power pellet 섭취, 고스트와 충돌(피격 또는 고스트 포식) 처리.

### 5.3 Ghosts (4종, 정통 알고리즘)
공통 상태: **Scatter → Chase → Frightened → Eaten(눈만 남아 하우스로 귀환) → Chase 복귀**

| Ghost | 별명 | Chase 목표 타일 로직 |
|---|---|---|
| Blinky | Shadow(빨강) | Pac-Man의 현재 타일을 직접 추적 |
| Pinky | Speedy(분홍) | Pac-Man 진행 방향으로 4칸 앞을 목표 (원작의 "위쪽 방향 시 4칸 앞+4칸 왼쪽" 오버플로우 버그 재현) |
| Inky | Bashful(청록) | Blinky 위치 → Pac-Man 2칸 앞 지점을 잇는 벡터를 2배 연장한 지점 추적 (4마리 중 가장 복잡, Blinky 상태에 의존) |
| Clyde | Pokey(주황) | Pac-Man과의 거리가 8타일 초과면 직접 추적, 이하면 자신의 Scatter 코너로 도주 |

- Scatter 상태에서는 각자 미로 모서리(자신의 홈 코너)로 이동.
- Power Pellet 섭취 시 전원 Frightened(색 반전/파랑, 저속, 랜덤에 가까운 회피 이동, 사망 직전 깜빡임)로 전환, Pac-Man이 닿으면 포식(점수 획득) 후 눈만 남아 하우스로 귀환.
- 연속 포식 시 점수 배수 증가(다음 Power Pellet 섭취 시 배수 리셋) — [확인필요] 정확 수치는 §8 참고.
- 고스트 하우스 출입: 초기엔 하우스 안에서 대기, 이후 순차적으로(또는 시간 기반으로) 방출 — 정확한 방출 조건(도트 카운터 vs 타이머)은 [확인필요], 1차 구현은 타이머 기반으로 단순화.
- "Cruise Elroy"(잔여 dot이 적을 때 Blinky 가속) 등 추가 authenticity 요소는 stretch로 검토, PLAN.md에서 포함 여부 확정.

### 5.4 아이템 & 점수
- Dot: 소량 점수.
- Power Pellet: dot보다 큰 점수 + Frightened 트리거.
- Ghost 포식: 연속 포식 시 배수 증가하는 점수.
- Fruit 보너스 아이템: **stretch 목표.** 포함 여부와 점수는 PLAN.md에서 확정(간단한 단일 아이템으로 단순화 가능).
- 정확한 점수 수치는 §8 [확인필요] 참고 — 코드에서는 상수 테이블로 분리해 한 곳에서 튜닝 가능하게 한다.

### 5.5 목숨 / 게임오버 / 승리조건
- 목숨 3개로 시작, 고스트에게 잡히면 1 감소 후 미로 초기 배치로 리스폰.
- 목숨 0에서 게임오버 화면(최종 점수 + 재시작 버튼).
- 특정 점수 도달 시 목숨 추가(1UP) 여부/기준점은 [확인필요], 기본 포함하되 정확 임계값은 튜닝 지점으로 표기.
- 레벨 클리어 시 다음 레벨로 자동 진행(§5 승리 조건 참고).

### 5.6 UI/HUD
- 현재 점수, high-score(세션 내), 남은 목숨, 현재 레벨 표시.
- 시작 화면(플레이 시작 안내), 일시정지, 게임오버/레벨클리어 오버레이.

## 6. 아키텍처 방향성 (상세 설계는 PLAN.md)

- Game loop: fixed timestep 로직 업데이트 + rAF 렌더링 여부는 Phase 1에서 트레이드오프 분석 후 결정.
- Ghost state machine, grid 기반 충돌 판정, 타이밍 테이블 구조를 코드 작성 전에 PLAN.md에서 설계.
- 모든 튜닝 가능한 수치(속도, 타이밍, 점수)는 하나의 config/constants 모듈로 모아 [확인필요] 항목을 쉽게 찾고 조정할 수 있게 한다.

## 7. 배포 요구사항

- 대상: Vercel(정적 프로젝트, 빌드 커맨드 불필요).
- 로컬 미리보기 방법과 `vercel` CLI 배포 절차(로그인/링크/`--prod`)를 PLAN.md와 CLAUDE.md에 구체적으로 명시.
- 배포 후 실제 발급된 인터넷 URL에 접속해 정상 플레이되는지 확인하는 절차를 PLAN.md의 검증 체크리스트에 포함.
- 배포(및 git push 등 외부 영향 작업)는 실행 직전 반드시 사용자 확인을 받는다.

## 8. [확인필요] 목록 (아케이드 오리지널 정확 수치 — 불확실, PLAN.md에서 근사치 확정 + 코드 내 튜닝 지점 지정)

- Scatter/Chase 각 페이즈 지속시간 및 레벨별 변화 패턴.
- Frightened 지속시간(레벨이 오를수록 짧아짐) 및 깜빡임(경고) 타이밍.
- Pac-Man/Ghost 기본 이동 속도, 터널 내 감속 비율, 레벨별 속도 증가 폭.
- Ghost 하우스 방출 조건(도트 카운터 기반 vs 타이머 기반) 및 순서.
- 점수표 정확 수치: dot/power pellet/ghost 연속 포식 배수/fruit 종류별 점수.
- 1UP(목숨 추가) 임계 점수.
- "Cruise Elroy" 등 부가 규칙의 정확한 발동 조건.

## 9. Out of scope (이번 버전 제외)

- 모바일 터치 컨트롤, 사운드, 온라인 랭킹/백엔드, 멀티플레이어, 레벨 에디터, 로컬 스토리지 기반 영구 하이스코어.

## 10. 다음 단계

Phase 1(plan mode)로 진입해 PLAN.md(아키텍처·자료구조·검증 체크리스트·배포 계획) + CLAUDE.md를 작성한다. **사용자 승인 후 진행.**
